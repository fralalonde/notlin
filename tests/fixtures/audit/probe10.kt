suspend fun fetch(): String = "data"
external fun nativeThing(): Int
fun useLists(xs: List<Int>, m: Map<String, Int>) {
    val keys = m.keys
    val first = xs.first()
    val up = "abc".uppercase()
}