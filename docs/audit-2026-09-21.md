# notlin silent-drop audit — findings summary (from deleg_657bbc18 audit, re-verified at HEAD 38f4399+)

Findings where notlin exits `0 error(s), 0 warning(s)` but output is wrong/missing:

## CRITICAL (silently wrong output)
1. Class type parameters dropped
   - unit.rs transpile_type_decl never reads `type_parameters` (functions do).
   - Trigger: `class Gen<T>(val x: T)` -> `public class Gen { private T x; ... }`, `T` undeclared -> javac "cannot find symbol". `/tmp/probe6.kt`
2. Function-typed / non-user_type primary-ctor params silently dropped
   - unit.rs: filter_map over class params keeps only user_type/nullable_type; function_type, parenthesized_type etc. are dropped from fields/ctor without diagnostic.
3. (check assistant| line 208-209 in delegation log for full list probe samples /tmp/probe*.kt)

## Known-list gaps (call/navigation pass-through, no diagnostic)
   - expr.rs navigation_call: verbatim, no warn
   - Triggers: `xs.first()` -> `xs.first()`, `"abc".uppercase()` -> `"abc".uppercase()`, `intArrayOf(1,2,3)` verbatim; `m.keys` -> `m.keys()` (Map uses keySet()); `arr.size` -> `arr.size()` (arrays use .length) — all javac errors with 0 diagnostics. probes 10/11.

## Notes
- cargo test 15/15 cannot detect any of these (asserts only errors==0, non-empty files, brace balance, a few contains).
- Audit child ran without JDK/env; javac gate claims should be re-checked locally.

## Fix priorities
P0: class type_parameters (fix mirrors function type_params handling)
P1: record class ctor param types into var_types — enables == on ctor params + ordered comparisons on them
P1: class_parameter filter must also accept function_type/parenthesized_type, else synthesize Object (or taint). Also warn when param dropped.
P1: navigation known-list misses -> P2: warn in navigation_call fallthrough when name is in a kotlin-stdlib-but-not-Java list. Emit N002 for calls not in a Java-known allowlist.
